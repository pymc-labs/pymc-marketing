"""Utilities for backwards compatibility testing.

Note: The `replace_half_flat_priors` function has been removed since
HalfFlat and Flat distribution replacement is now handled automatically
by the `mock_sampling()` context manager in `mock_pymc.py`.
"""
